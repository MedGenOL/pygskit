# Use this file to create a conda environment with the command: conda env create -f environment.yml
name: pygskit
channels:
  - defaults
  - conda-forge
  - bioconda
dependencies:
  - python>=3.10,<3.12
  - pip
  - pip:
      - click
      - hail
      - gnomad
